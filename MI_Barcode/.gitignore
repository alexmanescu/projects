# MI_Barcode — git ignore

# Credentials — never commit
config/config.php

# Dependencies (Composer vendor or manually downloaded libs)
vendor/
lib/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
*.idea/
.vscode/

# PHP temp / cache
*.log
